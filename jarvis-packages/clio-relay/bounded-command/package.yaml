name: clio-relay.bounded-command
description: Execute a bounded relay command under JARVIS-CD.
script: run.py
parameters:
  command:
    type: array
    required: true
  workdir:
    type: string
    required: false
  env:
    type: object
    required: false
  timeout_seconds:
    type: integer
    required: false
